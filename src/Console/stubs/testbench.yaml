laravel: '@testbench'

providers:
  # - {{ WorkbenchServiceProvider }}

migrations:
  - workbench/database/migrations

seeders:
  - {{ WorkbenchDatabaseSeeder }}

workbench:
  start: '/'
  install: true
  discovers:
    web: true
    api: false
    commands: false
    components: false
    views: false
  build:
    - asset-publish
    - create-sqlite-db
    - db-wipe
    - migrate-fresh
  assets:
    - laravel-assets
  sync: []
