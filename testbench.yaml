providers:
  - Orchestra\Workbench\WorkbenchServiceProvider
  - Workbench\App\Providers\WorkbenchServiceProvider

workbench:
  start: '/'
  build:
    - asset-publish
    - create-sqlite-db
    - migrate:refresh
  assets:
    - workbench-assets
